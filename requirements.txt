# --- Core Django ---
Django==6.0
dj-database-url>=2.1.0  # للاتصال بقاعدة البيانات في Render
psycopg[binary]>=3.1.18 # محرك قاعدة البيانات

# --- Environment ---
django-environ>=0.11.2
python-dotenv>=1.0.1

# --- Web Server (ASGI & WSGI) ---
daphne>=4.1.0           # لتشغيل WebSockets
whitenoise>=6.6.0       # لملفات الستاتيك
gunicorn>=21.2.0        # (اختياري)

# --- Real-time & Channels ---
channels[daphne]>=4.0.0
channels-redis>=4.2.0

# --- Celery & Background Tasks ---
celery>=5.3.6
redis>=5.0.1            # مكتبة بايثون للاتصال بـ Redis
django-redis>=5.4.0     # للكاش

# --- Azure Storage (مهم جداً للصور) ---
django-storages[azure]>=1.14.2
azure-storage-blob>=12.19.0

# --- AI & HTTP ---
openai>=1.12.0
requests>=2.31.0
httpx>=0.27.0           # طلبات غير متزامنة

# --- Utilities & Security ---
Pillow>=10.2.0          # معالجة الصور
django-csp>=3.7
django-axes>=6.0
nh3>=0.2.15             # تنظيف النصوص
cryptography>=42.0.0

# --- Admin UI ---
django-unfold>=0.19.0
django-import-export>=3.3.0



openpyxl